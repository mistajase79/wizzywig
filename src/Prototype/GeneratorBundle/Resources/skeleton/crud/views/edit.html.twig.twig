{% block extends %}
{{ "{% extends 'PrototypeAdminBundle:Control:admin.html.twig' %}" }}
{% endblock extends %}
{{ "{% form_theme edit_form 'bootstrap_3_layout.html.twig' %}" }}
{{ "{% block title %}" }}Update {{ entity_singularized|capitalize  }}{{ "{% endblock %}" }}
{{ "{% block subtext %}" }}Updates existing {{ entity_singularized|capitalize  }}{{ "{% endblock %}" }}

{% block body %}
{{ "{% block body %}" }}

    {{ '{{ form_start(edit_form) }}' }}
        {{ '{{ form_widget(edit_form) }}' }}
        <div>
            <button type="submit" class="btn btn-success"><span class="glyphicon glyphicon-ok" ></span>Update</button>
            <a href="{{ "{{ path('" ~ route_name_prefix ~ "_index') }}" }}" class="btn btn-primary">
                <span class="glyphicon glyphicon-triangle-left" ></span>Back
            </a>
        </div>
    {{ '{{ form_end(edit_form) }}' }}
    {{ include('crud/views/others/record_actions.html.twig.twig') }}
{{ "{% endblock body %}" }}
{% endblock body %}
