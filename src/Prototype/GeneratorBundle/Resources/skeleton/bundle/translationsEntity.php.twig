<?php

namespace Prototype\{{ shortname }}Bundle\Entity;

use Doctrine\ORM\Mapping as ORM;
use Gedmo\Translatable\Entity\MappedSuperclass\AbstractTranslation;

/**
 * @ORM\Table(name="{{ shortname|lower }}_translations", indexes={
 *      @ORM\Index(name="{{ shortname|lower }}_translation_idx", columns={"locale", "object_class", "field", "foreign_key"})
 * })
 * @ORM\Entity(repositoryClass="Gedmo\Translatable\Entity\Repository\{{ shortname }}Repository")
 */
class {{ shortname }}Translations extends AbstractTranslation
{
    /**
     * All required columns are mapped through inherited superclass
     */
}
